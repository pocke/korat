import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { StoreManager } from './renderer/component/StoreManager';

ReactDOM.render(<StoreManager />, document.getElementById('appmain'));
